# Image generation provider. Leave unset to use the built-in offline placeholder generator.
# When a real provider is wired up in src/lib/image-generator.ts, set this and its key.
IMAGE_PROVIDER=

# Example for a future Vercel AI Gateway integration:
# IMAGE_PROVIDER=gateway
# AI_GATEWAY_API_KEY=
